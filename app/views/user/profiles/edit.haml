= simple_form_for(current_user, url: profile_path, data: { turbo: false }) do |f|
  -# = f.error_notification
  -# = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?
  -# - if post.errors.any?
  -#   #error_explanation
  -#     %h2
  -#       = pluralize(post.errors.count, "error")
  -#       prohibited this user from being saved:
  -#     %ul
  -#       - post.errors.full_messages.each do |message|
  -#         %li= message
  = f.input :bio_information, input_html: { rows: "3" }
  = f.input :email
  = f.input :username
  = f.input :first_name
  = f.input :middle_name
  = f.input :last_name
  = f.button :submit, 'Save', class: 'button expanded font-bold'